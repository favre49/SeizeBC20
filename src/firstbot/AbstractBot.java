package firstbot;
import battlecode.common.*;

public abstract strictfp class AbstractBot
{
    // List of all directions. We should probably ignore Direction.CENTRE?
    static Direction[] directions = Direction.allDirections();

    // We could have made the run() method abstract but Java sucks and we can't have nice things.
}